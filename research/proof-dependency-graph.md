# Proof dependency graph

[E05 — Theorem dependency graph and blocked obligations](../book/diagrams/e05-theorem-dependencies.txt) is the canonical graph. It separates five theorem families, labels scoped dependencies, and preserves the exact points at which a stronger claim is blocked.

The depth hierarchy and trace-count branches are blocked by explicit proof obligations rather than treated as foundations for later claims.
