Two Transformer blocks carry distinct caches. Layer-one K,V come from normalized layer-one inputs; layer-two K,V come from normalized layer-two inputs. Each current chunk reads previous entries and appends new entries at its own layer. All histories have the same positional length; their content is not interchangeable. The reference concatenates without detaching to permit gradient comparison.
